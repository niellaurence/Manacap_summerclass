# TODO

- [ ] Gather understanding of current Flutter/Dart entrypoint and file structure.
- [ ] Propose implementation plan for Dice Roll + Toss Coin mini-games.
- [ ] Confirm plan with user.
- [ ] After approval, add a new Dart screen (or replace main) that implements the requested flow.
- [ ] Wire the new screen into `lib/main.dart` so the app runs the mini-games.
- [ ] Run `flutter analyze` and `flutter test` (if available) to ensure no errors.
- [ ] Provide a command to run the app.
